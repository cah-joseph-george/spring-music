---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 8u141-jdk

inputs:
  - name: source

outputs:
  - name: build

run:
  path: source/ci/tasks/build-jar.sh
